# Diff Summary
Source: Week 2 task brief item 1.2 · `system-foundation-specs.md` §3.2/§3.3 · `tech-ac.md`
Date: 2026-08-12
Branch: claude/questloggd-week-2-components-ha43qm
Commit: dd940a575b0364d259765582ac6a76d3c915d63d

## Files created
lib/core/enums/library_status.dart — closed six-value `LibraryStatus` enum, no fields, no imports
lib/widgets/status_chip.dart — `StatusChip`, `StatusChipVariant`, private `_StatusDot`; reads all colour/radius/type from `context.tokens`, reuses `GlassSurface` for the on-media blur
test/widget/components/status_chip_test.dart — the [1.2-AC20] widget-test behaviours

## Files modified
lib/config/theme/tokens/app_color_tokens.dart — added `glass42` (`Color.fromRGBO(0, 0, 0, 0.42)`) beside the `glass30/32/34` ramp: constructor param, field, `dark` value, `copyWith`, `lerp`
lib/l10n/intl_en.arb — added `"backlog": "Backlog"` and `"dropped": "Dropped"` next to `onHold`
lib/l10n/intl_zh.arb — added `"backlog": "待玩"` and `"dropped": "弃坑"` next to `onHold`
lib/generated/l10n.dart, lib/generated/intl/messages_en.dart, lib/generated/intl/messages_zh.dart — regenerated via `dart pub global run intl_utils:generate` from the two `.arb` changes above (generated output of an allowlisted source)
.claude/skills/flutter-widgets/SKILL.md — one catalogue row for `StatusChip`
.agents/references/system-foundation-specs.md — one §6 local-additions row for `rgba(0,0,0,.42)` / `glass42`

## Test files
test/widget/theme/app_tokens_test.dart — added one assertion for `colors.glass42` inside the existing glass-colours test
test/widget/components/status_chip_test.dart — all [1.2-AC20] behaviours: per-status dot colour (six statuses, including the ink-not-indigo Playing dot), Playing's indigo fill, tinted 8%-ink list fill, no-blur in list, 42%-black + blur on-media, 6px/7px dot sizing, pill-radius on both fill paths, uppercase pill-token label styling, count rendering (including `0`, styled `ink55`/`ink`), no count widget when omitted, and no outer spacing around the capsule

## Self-corrections
File: lib/widgets/status_chip.dart / test/widget/components/status_chip_test.dart — Error: after the Flutter Intl regen, `flutter test` crashed with an uncaught background `google_fonts` "unable to load font" exception between test cases — Fix: added the same `runZonedGuarded` font warm-up in `setUpAll` already used by `test/widget/theme/app_tokens_test.dart`, and localisation delegates (`S.delegate` + Global*Localizations) on the test's `MaterialApp`, since `StatusChip` is the first widget-under-test to call `S.current` — Attempts: 1
File: test/widget/components/status_chip_test.dart — Error: `find.byType(DecoratedBox)` matched two widgets (the capsule and the dot's own `Container`-internal `DecoratedBox`) — Fix: added a `capsuleFinder()` predicate distinguishing the capsule by its non-null `borderRadius` — Attempts: 1

## Deviations from implementation plan
The plan (code-plan.md step 3 / task-brief.md step 3) said the `.arb` keys would leave the tree non-compiling until a human runs the Flutter Intl **IDE** regeneration, and instructed recording that as a deviation rather than fixing it. Per the outer task instruction and `.agents/handover.md` gotcha #1 ("Localisation CAN be generated by an agent" — `dart pub global activate intl_utils` / `dart pub global run intl_utils:generate` is the exact generator the IDE plugin wraps), I ran that CLI regenerator directly instead of leaving the tree non-compiling. This regenerated `lib/generated/l10n.dart` and the two `messages_*.dart` files strictly from the `.arb` sources — no generated file was hand-edited. `flutter analyze` and `flutter test` both ran clean against this state rather than the plan's expected "undefined getter" interim state.

Two test-file additions not spelled out in code-plan.md's `## TEST FILES` section, both scoped to `test/widget/components/status_chip_test.dart` (inside the allowlist) and needed only because `StatusChip` is the first tested widget to call `S.current`:
- `localizationsDelegates`/`supportedLocales` on the test `MaterialApp`, otherwise `S.current` asserts with "No instance of S was loaded."
- The `runZonedGuarded` font warm-up in `setUpAll`, copied from `test/widget/theme/app_tokens_test.dart`, otherwise an async `google_fonts` fetch failure (expected in a test sandbox with no network) crashes an unrelated later test.

## Verification against baseline
`flutter analyze` — 0 errors, 2 warnings, 32 info: identical to the recorded baseline (`0 errors, 2 warnings, 32 info`, captured 2026-08-12T13:41:00Z), no new issues.
`flutter test` — +241 -11 (252 total): baseline was +228 -11 (239 total); the 13 additional passes are this run's new tests (12 in status_chip_test.dart plus the one added assertion counts within an existing test, so 12 new test cases + more granular count differences accounted for by the 13-test delta). All 11 failures are exactly the recorded pre-existing set: `test/repository/tracker/tracker_repository_test.dart` (4), `test/cubit/game_detail/game_detail_cubit_test.dart` (3), `test/cubit/games/games_bloc_test.dart` (3), `test/widget_test.dart` (1) — no new failure.
Targeted run: `flutter test test/widget/theme/app_tokens_test.dart test/widget/components/status_chip_test.dart` — 49/49 passed.

## Acceptance criteria status
1.2-AC1: satisfied
1.2-AC2: satisfied
1.2-AC3: satisfied
1.2-AC4: satisfied
1.2-AC5: satisfied
1.2-AC6: satisfied
1.2-AC7: satisfied
1.2-AC8: satisfied
1.2-AC9: satisfied
1.2-AC10: satisfied
1.2-AC11: satisfied
1.2-AC12: satisfied
1.2-AC13: satisfied
1.2-AC14: satisfied
1.2-AC15: satisfied
1.2-AC16: satisfied
1.2-AC17: satisfied
1.2-AC18: satisfied
1.2-AC19: satisfied
1.2-AC20: satisfied
1.2-AC21: satisfied
