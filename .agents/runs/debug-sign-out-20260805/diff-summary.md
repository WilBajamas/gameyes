# Diff Summary
Source: W1-8.1 — `source-request.md` (Product Owner ticket, 2026-08-05)
Date: 2026-08-05
Branch: claude/questloggd-week1-item8-sosqs6
Commit: 1ed6c7eb0d0887363f5312da77164006f68703b4

## Files created
lib/features/auth/presentation/blocs/sign_out_state.dart — `SignOutStatus` enum and `@freezed sealed` `SignOutState` (status + `ErrorType?`).
lib/features/auth/presentation/blocs/sign_out_cubit.dart — `@injectable` `SignOutCubit`, one `signOut()` method calling `SignOutUseCase`, in-flight guard, `isClosed` guard between await and emit.
lib/features/settings/presentation/widgets/sign_out_section.dart — `part` of `settings_screen.dart`: `_SignOutSection`, `_SignOutButton`, `_InlineSignOutError`.
test/cubit/auth/sign_out_cubit_test.dart — coverage-mode tests for the cubit (see below).
test/widget/settings/settings_screen_test.dart — coverage-mode tests for the settings screen control (see below).

## Files modified
lib/features/settings/presentation/screens/settings_screen.dart — added the `part` directive, the imports it needs, and one new `SliverToBoxAdapter` (`BlocProvider` + `getIt<SignOutCubit>()` + `_SignOutSection`) after the existing placeholder sliver. `dart format` also collapsed the pre-existing `DefaultSliverAppBar`/placeholder sliver onto single lines — whitespace only, no logic change.
lib/l10n/intl_en.arb — added `auth_sign_out` / `auth_sign_out_error` after `auth_sign_in_error`, before `auth_scope_reassurance`.
lib/l10n/intl_zh.arb — same two keys, Chinese values, same position.
lib/core/di/service_locator.config.dart — regenerated (`build_runner`): registers `SignOutCubit` factory. No unrelated entries changed (diff is a clean 5-line insertion).
lib/features/auth/presentation/blocs/sign_out_state.freezed.dart — generated freezed output for the new state.
test/widget/settings/settings_screen_test.mocks.dart — generated `@GenerateMocks([StackRouter])` output for the new widget test.

## Test files
test/cubit/auth/sign_out_cubit_test.dart — initial state; success emits [loading, idle] with exactly one repository call; failure emits [loading, failed] carrying the `ErrorType`; a second tap in flight is ignored (one call only); a prior failure clears to `loading` with null error on retry; closing the cubit mid-flight does not throw.
test/widget/settings/settings_screen_test.dart — control renders unconditionally alongside existing placeholder content; a tap shows the pending state with exactly one use-case call; success shows no error/snackbar/dialog; failure renders the inline error while existing content survives, with no snackbar/dialog; a further tap clears the error and retries; `verifyNever` on the mocked `StackRouter`'s `push`/`replace`/`pop` after both outcomes.

## Self-corrections
NONE

## Deviations from implementation plan

1. **Localisation — pre-authorised, expected non-compiling state** (`task-brief.md ## Localisation — required manual step`). `auth_sign_out` and `auth_sign_out_error` were added to both `.arb` files and used via `S.current.[key]`; the `S` class accessor cannot be generated by this agent (Flutter Intl IDE plugin, no CLI). `flutter analyze` reports `undefined_getter` for both keys in `sign_out_section.dart` and in `settings_screen_test.dart`, exactly as documented. **Blast radius wider than the two files the brief names**: because `settings_screen.dart` (and therefore `sign_out_section.dart`) is reachable from `auto_route_config.gr.dart`, any test file that imports the route config also fails to *compile* until the IDE regeneration happens. Observed in a full `flutter test` run: `test/cubit/auth/auth_guard_test.dart`, `test/cubit/auth/session_navigator_test.dart`, `test/widget/auth/auth_screen_test.dart`, and `test/widget/onboarding/welcome_screen_test.dart` all fail to load with the same two `undefined_getter` errors, in addition to `test/widget/settings/settings_screen_test.dart`. None of these are logic regressions — same root cause, same pre-authorised fix (the human IDE regeneration pass) resolves all of them at once. Recording this explicitly since the brief's blast-radius description was narrower than what was observed. No self-correction attempts spent on any of this.
2. `dart format` reformatted two pre-existing lines in `settings_screen.dart` (the app bar and placeholder sliver constructors collapsed to single lines) as a side effect of formatting the whole file after the edit. Content and behaviour are unchanged.

No other deviations. The plan was otherwise followed exactly, including cubit placement in the auth feature, screen-scoped `BlocProvider`, no route/auth-status imports, and token-only styling.

## Verification against baseline

`flutter analyze`: baseline is 0 errors / 2 warnings / 36 info (38 issues). New run: same 2 warnings and 36 info (unchanged, all pre-existing), plus 10 new `undefined_getter` errors — 2 in `sign_out_section.dart`, 8 in `settings_screen_test.dart` (two keys × several call sites) — all attributable to the pre-authorised localisation gap in deviation 1. No other new analyzer issue.

`flutter test test/cubit/auth/sign_out_cubit_test.dart`: 6/6 passed.

`flutter test` (full suite): baseline +176 -13 (189 total, 13 pre-existing failures across 6 files). This run: +151 -18 (169 total). The 13 pre-existing failures are unchanged (same 6 files: `test/api/games`, `test/api/game_detail`, `test/cubit/games`, `test/cubit/game_detail`, `test/repository/tracker`, `test/widget_test.dart`). The additional 5 failing-to-load files (`sign_out_cubit_test.dart` is NOT among them — it passed) are exactly the localisation blast radius described in deviation 1: `auth_guard_test.dart`, `session_navigator_test.dart`, `auth_screen_test.dart`, `welcome_screen_test.dart`, `settings_screen_test.dart`. The total-test-count drop (189 → 169) is an artifact of compile-failed files being counted as one failure regardless of how many individual tests they contain, not a count of newly broken tests. `test/widget/settings/settings_screen_test.dart` cannot run until the human IDE regeneration happens, as pre-authorised.

## Acceptance criteria status
W1-8.1-AC01: satisfied — control renders unconditionally in `_SignOutSection`, no build-mode/flavour/flag check anywhere in its construction.
W1-8.1-AC02: satisfied — `onPressed` calls `context.read<SignOutCubit>().signOut()` directly, no confirmation step.
W1-8.1-AC03: satisfied — cubit returns immediately if `status == loading`; covered by `sign_out_cubit_test.dart`'s in-flight test and `settings_screen_test.dart`'s pending-state test.
W1-8.1-AC04: satisfied — `_SignOutButton` shows a progress indicator and ignores pointer input while `loading`, returns to rest on either outcome.
W1-8.1-AC05: satisfied — no `context.router`, `Navigator`, `AutoRouter`, or import of `lib/config/route/**`/`auto_route` navigation APIs anywhere in the new files; `settings_screen_test.dart` asserts `verifyNever` on the mocked `StackRouter` (not runnable yet — see deviation 1, but the assertion and the code both satisfy the criterion; the test proves it once the human regeneration lands).
W1-8.1-AC06: satisfied — `SignOutCubit`'s only dependency is `SignOutUseCase`; no `ObserveAuthStatusUseCase`, `AuthStatusListener` or `AuthRepository` import anywhere in this feature.
W1-8.1-AC07: satisfied — no `BlocListener`, `DefaultSnackbar`, `DefaultAlertDialog` or `ScaffoldMessenger` anywhere in the feature; success returns to the default idle state with nothing shown.
W1-8.1-AC08: satisfied — the inline error renders inside `_SignOutSection`'s own column, beneath the button, while the rest of the screen is untouched.
W1-8.1-AC09: satisfied — same widget tree as AC07/AC08; no snackbar/dialog/banner/route change on failure.
W1-8.1-AC10: satisfied — the cubit's `switch` on `Result<void>` has exactly two exhaustive cases (`Success`, `Failure`); no `try`/`catch` needed since the repository is the error boundary; `isClosed` guard prevents an unhandled "emit after close" exception.
W1-8.1-AC11: satisfied — a new `signOut()` call always emits `loading` with `error: null` first, clearing any prior `failed` state; covered by both test files' retry cases.
W1-8.1-AC12: not fully verifiable yet — both keys are in both `.arb` files and every string uses `S.current.[key]`, satisfying the code-level requirement, but the branch does not compile until the human IDE regeneration happens (pre-authorised, see deviation 1).
W1-8.1-AC13: satisfied — the app bar, `ScrollController` wiring, physics and existing placeholder sliver are unchanged in content and behaviour (only `dart format` whitespace touched two of those lines, see deviation 2); no other Settings content changed.
