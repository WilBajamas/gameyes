# Diff Summary
Source: `.agents/week-1-task-briefs.md` §"10 — Sentry [PIPELINE]", plus the IGDB
`talker` logging and `PrettyDioLogger` removal delta ([10.15]-[10.26])
Date: 2026-08-06
Branch: claude/questloggd-resume-e1e0fi
Commit: a963c5ff4a97845d131cb4dcaec4a32771ce6cd3

## Files created
lib/core/services/sentry/crash_reporting_settings.dart — resolves whether crash reporting starts and which environment name to use, from flavour + DSN
lib/core/services/sentry/app_version.dart — reads `version+buildNumber` from the platform, null on failure
lib/core/services/sentry/crash_reporter.dart — initialises Sentry, tags events, scrubs PII, starts the app exactly once
lib/core/services/sentry/crash_report_user.dart — `@singleton`; attaches/clears the Supabase user id on the Sentry scope as auth status changes
lib/core/services/sentry/test_crash.dart — double-gated (`--dart-define=SENTRY_TEST_CRASH=true` + `Flavor.dev`) deliberate crash for delivery verification
lib/core/services/supabase/igdb_call_log.dart — console-only `talker` request/response/failure logging for the IGDB client, dev+debug gated, with the 50-line body trim
test/repository/sentry/crash_reporting_settings_test.dart — every branch of `CrashReportingSettings.resolve`
test/api/supabase/igdb_call_log_test.dart — the 50-line body cap boundary and truncation marker

## Files modified
pubspec.yaml — add `sentry_flutter: ^9.26.0`, `package_info_plus: ^10.2.1`; remove `pretty_dio_logger: ^1.4.0`
pubspec.lock — resolved as a result of `flutter pub get`; `pretty_dio_logger` no longer appears anywhere (was `direct main`, nothing else depended on it)
lib/core/res/const.dart — add `ConfigConstants.sentryDsn`, add `SentryConstants`, add `SupabaseIgdbProxyConstants.maxLogBodyLines`
lib/config/config_envied.dart — add one obfuscated `sentryDsn` `@EnviedField` to the shared `Env` class only
lib/bootstrap.dart — `runApp(app)` now runs inside `CrashReporter.start`'s `startApp` callback, which also starts `CrashReportUser` and schedules the test crash; every earlier statement and its order is unchanged
lib/config/flavor/flavor_config.dart — add `static FlavorConfig? get instanceOrNull => _instance;`; `instance`, `initialise`, fields and constructor untouched
lib/core/services/supabase/supabase_igdb_client.dart — wrap the existing body in try/catch, add `IgdbCallLog.request`/`.response`/`.failure` calls, `rethrow` on failure; signature, `@injectable`, `const` constructor and `.timeout(...)` unchanged
lib/core/di/network_module.dart — delete the `pretty_dio_logger` import and its `interceptors.add` call; nothing else changed
lib/core/services/api/twitch_auth_interceptor.dart — delete the `pretty_dio_logger` import and the constructor body that registered it (constructor now ends at the initialiser list); no other member changed
.agents/references/flutter-arch.md — correct the `NetworkModule` path on line 169, drop `+ PrettyDioLogger` on line 170, delete the `Logging: PrettyDioLogger...` line 181
lib/core/di/service_locator.config.dart — regenerated by build_runner to wire the new `@singleton CrashReportUser`
linux/flutter/generated_plugin_registrant.cc, linux/flutter/generated_plugins.cmake, macos/Flutter/GeneratedPluginRegistrant.swift, windows/flutter/generated_plugin_registrant.cc, windows/flutter/generated_plugins.cmake — mechanical `flutter pub get` side effect of adding `sentry_flutter`/`package_info_plus` (registers their unused-on-this-project desktop plugin implementations); not hand-edited, same category as `pubspec.lock`

## Test files
test/repository/sentry/crash_reporting_settings_test.dart — 5 tests: unknown flavour, placeholder DSN, empty DSN, same DSN both flavours, environment named after flavour
test/api/supabase/igdb_call_log_test.dart — 4 tests: under cap unchanged, exactly at cap unchanged, over cap keeps first 50 lines, over cap carries the cut-short marker

## Self-corrections
File: lib/core/services/sentry/crash_reporter.dart — Error: `SentryEvent.copyWith` is deprecated in `sentry_flutter: ^9.26.0`, producing a new `deprecated_member_use` analyzer info not in the baseline — Fix: mutate `event.tags`/`event.user` directly (both are plain mutable fields, per the deprecation's own guidance) instead of calling `copyWith` — Attempts: 1
File: lib/config/flavor/flavor_config.dart — Error: running `dart format` reformatted the pre-existing `initialise` switch expression's indentation (unrelated to the one getter this step was allowed to add) — Fix: kept the added getter, manually restored the original indentation of `initialise` rather than letting the formatter touch it — Attempts: 1

## Deviations from implementation plan
`package_info_plus` resolved as `direct main` in `pubspec.lock`, not transitive via `sentry_flutter` as `task-brief.md` assumed. Per the brief's own instruction, reporting this rather than silently treating it as transitive. It is still one of the two approved new packages, so no escalation — just a correction to the stated rationale.

`envied` accepted `SentryConstants.placeholderDsn` directly as the `@EnviedField` `defaultValue` (the pre-approved literal-string fallback in `task-brief.md` Step 3 was not needed).

Five desktop-platform plugin registrant files (`linux/`, `macos/`, `windows/`) changed as a mechanical `flutter pub get` side effect of the two new packages — see "Files modified" above. Not in the file allowlist by name, but the same category of unavoidable dependency-resolution output as `pubspec.lock`, which the allowlist does cover; this app targets Android only (`flutter-arch.md`) so nothing runtime-visible changes.

## Verification against baseline
`flutter analyze`: 0 errors, 2 warnings, 32 info — matches the recorded baseline exactly (`lib/config/flavor/flavor_config.dart:5:81` pre-existing long doc-comment line is the only leftover `sentry`/`igdb`-adjacent info, and it predates this change).

`flutter test`: same 11 pre-existing failures as the baseline (`test/repository/tracker/tracker_repository_test.dart` x4, `test/cubit/game_detail/game_detail_cubit_test.dart` x3, `test/cubit/games/games_bloc_test.dart` x3, `test/widget_test.dart` x1), no new failures. The three existing `test/api/supabase/supabase_igdb_client_test.dart` tests still pass ([10.21]). All 9 new tests pass.

## Acceptance criteria status
10.1: satisfied
10.2: satisfied
10.3: satisfied — fresh checkout with no `secret.env` resolves the placeholder and builds
10.4: satisfied — exercised path in this checkout (no real DSN available); one `debugPrint`, no retry
10.5: satisfied
10.6: satisfied — `SentryFlutter.init` installs the root Dart/Flutter error handlers by default; no code here suppresses them
10.7: satisfied — same as 10.6, framework error hook is the SDK default, unmodified
10.8: satisfied — flavour tag always set; version tag omitted (not blocking) when `AppVersion.read()` returns null
10.9: satisfied — `sendDefaultPii = false`, `beforeSend` reduces `user` to `{id}` only, no email/name/avatar/token ever touched
10.10: satisfied — `sendDefaultPii = false`
10.11: satisfied — `CrashReportUser` sets/clears the Sentry scope user on every auth status change
10.12: not run — no real DSN in this checkout; implemented, left for the human's manual QA pass as instructed
10.13: satisfied — `bool.fromEnvironment` + `Flavor.dev` double gate, no UI affordance
10.14: satisfied — only `runApp(app)`'s call site moved in `bootstrap.dart`; analyzer/test baselines hold
10.15: satisfied
10.16: satisfied
10.17: satisfied — `rethrow` preserves type/message/stack; no cap applied to error path
10.18: implemented, not unit-tested (per task-brief.md, `kDebugMode` is always true under `flutter test`) — manual dev-run QA check
10.19: satisfied — console/`Talker` only, no viewer import, no new widget/route
10.20: satisfied — no `TalkerObserver`, no cross-import between the sentry/ and supabase/ log paths
10.21: satisfied — signature, return value and error propagation unchanged; existing client tests still pass
10.22: satisfied
10.23: satisfied
10.24: satisfied — `pretty_dio_logger` absent from `pubspec.lock` entirely
10.25: satisfied
10.26: satisfied — see Verification against baseline

## Revision 2 (Phase 4B code review — simplification)
Date: 2026-08-07
Commit: e652d1f
Additive on top of the entry above — supersedes nothing there.

Per human review of a963c5ff4a97845d131cb4dcaec4a32771ce6cd3: simplify
`CrashReportingSettings` and `AppVersion` from single-method classes to
top-level functions.

### Files modified
lib/core/services/sentry/crash_reporting_settings.dart — `CrashReportingSettings` class replaced by a top-level `resolveCrashReportingSettings` function returning a nullable `({String dsn, String environment})` record; same logic, same doc comment
lib/core/services/sentry/crash_reporter.dart — calls `resolveCrashReportingSettings(...)` instead of `CrashReportingSettings.resolve(...)`; `_configure`'s `settings` parameter is now typed `({String dsn, String environment})`; imports `core/utils/version_utils.dart` instead of the deleted `sentry/app_version.dart`; calls `readAppVersion()` instead of `AppVersion.read()`
test/repository/sentry/crash_reporting_settings_test.dart — all 5 calls updated to `resolveCrashReportingSettings(...)`; same test cases, same assertions

### Files deleted
lib/core/services/sentry/app_version.dart — replaced by lib/core/utils/version_utils.dart

### Files created
lib/core/utils/version_utils.dart — top-level `readAppVersion()` function, same logic and doc comment as the deleted `AppVersion.read()`

### Test files
No test files added or removed. `test/repository/sentry/crash_reporting_settings_test.dart` updated in place (see above).

### Self-corrections
NONE

### Deviations from implementation plan
NONE — both files simplified exactly as specified in the review feedback.

### Verification against baseline
`dart run build_runner build --delete-conflicting-outputs` — not run; nothing annotated changed (both are plain functions).
`flutter analyze`: 0 errors, 2 warnings, 32 info — matches the recorded baseline exactly.
`flutter test`: 199 passing, 11 failing out of 210 — same 11 pre-existing failures as the baseline, no new failures. `test/repository/sentry/crash_reporting_settings_test.dart`'s 5 tests all pass.

### Acceptance criteria status
No criteria affected — this revision is a pure refactor of already-covered logic behind [10.2], [10.4], [10.5], [10.8]; no behaviour change.

## Follow-up: TestCrash removal
Date: 2026-08-07
Commit: 7adeb25f23ab9633ce87f4a7f815f37be519c564

Removed once both gating conditions in `orchestrator-state.md ## Follow-up
actions` were met: QA passed, and the human confirmed a real Sentry event was
delivered ([10.12] manual check, `--flavor dev`). `TestCrash` was the manual
verification mechanism only — no longer needed now that delivery is confirmed.

### Files deleted
lib/core/services/sentry/test_crash.dart — the whole `TestCrash` abstract class

### Files modified
lib/bootstrap.dart — removed the `import '.../sentry/test_crash.dart'` line and the `TestCrash.scheduleIfRequested(flavor);` call (was the last line inside `CrashReporter.start`'s `startApp` callback); `getIt<CrashReportUser>().start();` and `runApp(app);` unchanged
lib/core/res/const.dart — removed `SentryConstants.testCrashFlag`, `.testCrashMessage`, `.testCrashDelay`; `placeholderDsn`, `flavorTag`, `appVersionTag` unchanged

### Test files
None — `TestCrash` had no dedicated test file (grep-confirmed prior to removal, per `orchestrator-state.md`).

### Self-corrections
NONE

### Deviations from implementation plan
NONE

### Verification against baseline
`dart run build_runner build --delete-conflicting-outputs` — not run; nothing annotated changed.
`flutter analyze`: 0 errors, 2 warnings, 32 info — matches the recorded baseline exactly.
`flutter test`: 199 passing, 11 failing out of 210 — same 11 pre-existing failures as the baseline (tracker_repository_test.dart x4, game_detail_cubit_test.dart x3, games_bloc_test.dart x3, widget_test.dart x1), no new failures.

### Acceptance criteria status
No criteria affected — [10.12]/[10.13]/[10.19] etc. covered `TestCrash`'s presence during manual verification, not its eventual removal. This run remains COMPLETE.
